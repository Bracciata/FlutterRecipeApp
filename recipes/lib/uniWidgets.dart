import 'package:flutter/material.dart';

class ErrorWidgetUni extends StatelessWidget {
  ErrorWidgetUni({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      child: new Text('issue dawg'),
    );
  }
}
